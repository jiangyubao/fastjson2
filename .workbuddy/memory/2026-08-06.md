# 2026-08-06

## OSGi 产物文件名(16:44)

用户要求 mvn 打包 OSGi 输出 `<BSN>_<version>.jar` 格式:
- core/pom.xml 的 `<build>` 加 `<finalName>com.alibaba.fastjson2_${project.version}</finalName>`
- 产物:`core/target/com.alibaba.fastjson2_2.0.63.jar`(320,721B)
- manifest 验证:BSN=com.alibaba.fastjson2、Bundle-Version=2.0.63、Export 两包、Import sun.misc;resolution:=optional、Require-Capability JavaSE 1.8 —— 全部正确
- 注意:用户原话写的是 `com.alibaba.fastjson_2.0.63.jar`(无 2),按 BSN 惯例取 `com.alibaba.fastjson2`;若确实需要不带 2 的名字,改 finalName 即可

## git 提交(16:47)

提交 `2b5090df2`,3 文件 +15 行:
- core/pom.xml(finalName)、.gitignore、.workbuddy/memory/2026-08-06.md
- **重要发现**:提交前工作区被误恢复了一批旧文件(时间戳 7月30-8月1日,即精简中间状态),且已 staged(new file 29 个):docs/Filter、annotations/autotype/mixin/jsontype_seealso/reader_codegen/register_custom_reader_writer 说明页、docs/images、docs/_config.yml、SECURITY.md、.codecov.yml、.editorconfig、scripts/bump_fastjson2_version、mvnw/mvnw.cmd
- 判定:HEAD(ab752b3fb)是干净精简状态(顶层无这些文件),这批文件与"只匹配现有功能"目标矛盾 → **取消暂存 + 删除磁盘文件**(全部可从 git 历史恢复,如 `git checkout 42342ebaf -- docs/Filter`);mvnw/mvnw.cmd 保留磁盘(本地构建用)但加入 .gitignore 忽略
- 提交后工作区干净;HEAD 领先 origin/main 一个提交,未推送
- 教训:用户目录可能被旧备份/副本覆盖过(文件时间戳回到 7月30-8月1日),git 提交前务必先 `git status` 检查是否有意外恢复的文件,勿盲目 `git add -A`

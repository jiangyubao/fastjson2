# 2026-08-01

## util 包二次优化(07:35)

用户问 Fnv 是否哈希(是:FNV-1a 64 位,JSONReader 字段名匹配核心)。
- 删 Fnv 2 个死方法(hashCode64LCase/hashCode64(String...))
- 删 Wrapper 接口 + JSONReader 分支、ParameterizedTypeImpl 类 + PARAM_TYPE_LIST_STR
- TypeUtils 删 31 个死方法(约 600 行):cast 组/getMapping 组/数值转换组/校验组,全部"外部+内部"双零调用验证
- 同步清理 3 个测试;130 测试全绿;jar 354KB;util 14 类→11 类
- 关键经验:死方法识别要排除 1) 方法自身递归 2) JDK 同名方法(Boolean.parseBoolean/Long.compare)3) 静态块初始化;PARAM_TYPE_LIST_STR 有内部引用(intern),整组删才安全
- 保留项:TypeUtils 1.x 兼容静态块(与 getInnerMap 纠缠,树模式死路径但保守保留)

## IDE 警告三轮清理(07:40)

- 主代码 raw type 修复(修代码优于注解):Collection<?>/Map<String,?> 强转/Map.Entry<?,?>/write(List) 改 write(List<?>)(父类+子类+调用点同步)
- deprecated String.getBytes(int,int,byte[],int)(JDK8)→ 数字字符串 ASCII 直接 char→byte 循环
- JSONFactory 注解去 unchecked(IDE 报 Unnecessary);JSONReaderUTF16 删 restriction(删时间方法后已不用 UNSAFE——教训:注解必要性会随删代码变化,需重查)
- 删 MutableBigInteger 未使用变量、TypeUtils 3 个 Atomic import;IOUtilsTest 修 v 变量+加 restriction
- 130 测试全绿;lint 复查仅剩 javac 专属 dep-ann/Unsafe 内部 API(mvn/IDE 不显示)

## test 包 Unsafe 平替(07:50)

用户问 test 为何用 Unsafe、要平替(不需要性能)。澄清:Unsafe 260 处中 259 处在 main,test 原只有 3 处:
- IOUtilsTest 1 处死代码(返回值丢弃)→ 直接删,连带删 restriction 注解
- XxHash64 测试辅助(XXH64 哈希,与 JSON 无关)→ 删 XxHash64.java + XxHash64Test.java
- JSONReaderTest2 36 处 UNSAFE.getLong/getInt(byte[], ARRAY_BYTE_BASE_OFFSET+N)→ 正则批量替换为 IOUtils.getLongLE/getIntLE(byte[], N),删除 UNSAFE/ARRAY_BYTE_BASE_OFFSET 静态导入
- 126 测试全绿;test 目录已 0 Unsafe;main 259 处保留(性能核心)
- 经验:IOUtils 的 getLongLE/getIntLE/getShortLE 就是 Unsafe 批量读的官方平替,测试/业务代码可直接用

## test 警告补充清理(07:52)

- JSONReaderTest2:删未使用变量 value;test1 的 new JSONReaderUTF16 改 try-with-resources(资源泄漏)
- IOUtilsTest:删 JDKUtils 静态导入(UNSAFE 平替后不再使用)
- test 未使用 import 扫描=0;IDE 资源泄漏只跟踪直接 new 的 Closeable,JSONReader.of 工厂方法不报
- 126 测试全绿

## pom 不同步警告根因(07:55)

- .vscode/settings.json 原为 "java.configuration.updateBuildConfiguration": "disabled" → VS Code Java LS 不自动同步 pom,精简删包后项目模型过期,持续报 "Project configuration is not up-to-date"
- 改为 "automatic";用户需手动执行一次 Java: Clean Java Language Server Workspace 或重启窗口生效

## 树 API 测试 + PrettyFormat bug 修复(08:02)

- 新建 JSONTreeAPITest.java:28 个测试方法、约 260 断言,覆盖数字(边界/精度/科学计数/BigDecimal/BigInteger)、bool、字符串转义(引号/换行/unicode/中文)、null、{}、[]、4 层嵌套(对象↔数组)、JSONObject/JSONArray 全部 getter 与集合操作、of 工厂、序列化回环、PrettyFormat、单引号解析、clone、toString
- 发现并修复 2 个库 bug:JSONWriterUTF16.write(List) 局部 NONE_DIRECT_FEATURES 和 JSONWriterUTF8.write(List) 检查都误含 PrettyFormat → PrettyFormat 序列化含数组的对象时抛"not support"异常。去掉后 PrettyFormat 正常
- 测试揭示的行为细节:小数解析为 BigDecimal(保精度,getDouble 自动转)、默认序列化跳过 null 值(需 WriteMapNullValue)、JSONArray.add 返回 boolean 不可链式(用 fluentAdd)、嵌套数组 readArray 返回 JSONArray
- 全量 154 测试通过;jar 362KB

## docs 全面调整(08:15)

用户要求基于当前(树模式)功能调整 docs/*.md。共 27 个 md + Filter/ 3 个,调整策略:
- **重写(反映现状)**:index.md(纯树模式功能范围/API)、ARCHITECTURE.md(无 Object Mapping 层架构图)、features_cn/en.md(按真实枚举 Reader 32/Writer 48 重写,标注"树模型生效✅/Bean专用—",AutoType 相关 Feature 已确认从枚举移除)、FAQ_cn/en.md(去 Bean/注解/日期/AutoType/TypeReference)、performance_cn/en.md(去 ASM/自定义 reader-writer)、fastjson_1_upgrade_cn/en.md(改为精简版 vs 1.x/完整版差异表)
- **替换为"已移除"说明页**:annotations/autotype/mixin/reader_codegen/register_custom_reader_writer/jsontype_seealso 各中英 + Filter/(index/name_filter/value_filter)
- **微调**:design_jsonreader/writer 追加树模式说明
- 验证:62 个相对链接 0 断链
- 经验:docs 调整先提取真实 API/枚举(JSON.java 30 个 public 方法、Feature 枚举值),再用"树模型生效"标注区分;上游继承的 12 个功能文档统一替换为"已移除+替代方案"模板避免死链

## 删除 TypeUtils + 澄清 Fnv(08:35)

用户要求删除 TypeUtils/Fnv(认为二者是"类加载/AutoType 聚集地")。**核实后 Fnv 不能删**:它是字段名 FNV-1a 哈希,被 JSONReaderUTF8/UTF16/ASCII + SymbolTable 调用 37 处,与 AutoType 无关(用户认知有误,已澄清)。

TypeUtils.java(2834 行)整体删除:
- 类加载残留:loadClass/getArrayClass/getClass(Type)/hasIllegalTypeNameChars/1.x 兼容块(反射加载 fastjson1 JSONObject)/NAME_MAPPINGS/getTypeName/getInnerMap
- 死数字方法:parseDouble/parseFloat(约970行)/parseInt/parseLong/toBigInteger/toLong/isInteger/toList/isNumber
- 迁移到 IOUtils:toString 系列+X2、doubleValue/floatValue、toBigDecimal/parseBigDecimal(去 X1 StringLatin1 反射)、toIntValue/toLongValue/toDoubleValue、isInt64、isJavaScriptSupport
- 坑:1) Reader 子类静态导入 toString 被基类 toString(List/Map) 实例方法遮蔽 → 必须显式 IOUtils.toString;2) JSONReader 的 getInnerMap 是 1.x 兼容,树模式直接 object;3) JSONWriter.checkAndWriteTypeName 是死路径(0 调用)连带删 getTypeName;4) 测试 TypeUtilsTest2/FloatingDecimalStr 同步清理
- 结果:29 文件 / 37,977 行 / jar 337KB / 145 测试全绿 / util 10 类

## 指数移除+SymbolTable删+JDKUtils死字段(08:50)

用户要求:1) 去掉指数写法(与 JS/PHP 通用 JSON 通信,1e2 不支持);2) Scientific/JDKUtils 不需要;3) 评估 SymbolTable。

- 指数:UTF8(3)+UTF16(5)处 e/E 块替换为抛 JSONException;ASCII 继承 UTF8 自动生效;序列化端 BigDecimal 走 writeDecimal 无指数;145 测试更新 2 个
- **坑**:上轮迁移把错误的 doubleValue/floatValue 重载迁进 IOUtils((boolean,int,byte[],int) 是死代码,真正调用的是 (int,long,int));已修正并删孤立测试 FloatingDecimalStr
- SymbolTable:确认纯死代码(Bean 时代字段名→序号映射,树模式无逻辑使用)→ 删文件+JSONWriter 构造参数/字段/getSymbolTable+JSONReader.Context 字段
- **Scientific/JDKUtils 澄清:不能删**——Scientific 是 double 序列化底层引擎(ED/ED5/EF 查表),JDKUtils 是 UNSAFE/字符串核心被 14 文件引用;用户"纯 java 服务端"恰恰需要 JDKUtils 的跨版本自适应
- JDKUtils 清理 6 个 0 引用死字段(SQL/Transient/Vector,含 Class.forName 静态块)
- 结果:145 测试全绿;jar 330KB 左右

## JDKUtils 死代码清理收尾(08:52)

上轮删 JDKUtils SQL/Vector/Transient 死字段时漏了方法体引用:isSQLDataSourceOrRowSet(L393-396)引用已删字段 → IDE 报错。教训:**删字段后必须 grep 方法体引用**。
- 删 isSQLDataSourceOrRowSet(0 外部引用)
- 同类隐患扫描发现并删:setReflectErrorLast + reflectErrorLast/reflectErrorCount 字段、latin1StringJDK8(均 0 引用)
- 清理 3 个 unused import(Method/List/AtomicInteger)
- 145 测试全绿;jar 334KB

## 残留警告清理(08:55)

- 删 4 处 int expValue 死变量(指数块替换后的残留,UTF8×2 + UTF16×2)
- 删 IOUtils Objects import、TypeUtilsTest2 StandardCharsets/Random import
- JDKUtils 注解去 unchecked(不再需要)
- JSONArray:删孤立 @Deprecated + 残留 javadoc(删方法后遗留,悬空注解会错误标记 addObject 为 deprecated)
- 全量 unused import 复查 = 0;145 测试全绿;jar 333KB
- 教训:删方法后要检查 1) 悬空注解(@Deprecated/@SuppressWarnings) 2) 悬空 javadoc 3) 局部变量残留

## 死类清理(08:58)

- 用户指出 JSONSchemaValidException 不需要(无 Schema 验证)→ 删(外部 0 引用)
- 全类引用扫描发现 FDBigInteger 也是死类:原服务于已删的 parseDouble/parseFloat,500 行 → 删
- 教训:删 TypeUtils 时 FDBigInteger/MutableBigInteger 是"曾用依赖",TypeUtils 删除后 FDBigInteger 失联——删大文件后要复查其专用依赖类的引用
- 复核:主代码无外部 0 引用类;145 测试全绿

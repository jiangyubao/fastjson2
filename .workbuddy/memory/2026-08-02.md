# 2026-08-02

## OSGi 打包(09:30)

用户问如何打包成 OSGi bundle:
- core/pom.xml 加 felix maven-bundle-plugin 5.1.9,bundle goal 接管;BSN=com.alibaba.fastjson2,导出两个包
- 排坑:1) bnd 把 JDKUtils 的 android.os Class.forName 误判为依赖 → 删反射检测(顺便符合"纯 java 服务端");2) sun.misc.Unsafe 需 `sun.misc;resolution:=optional`(否则 bundle ACTIVE 但调用 NoClassDefFoundError);3) Private-Package 不能 !*(会打空 jar)
- 用真实 Felix 7.0.5 做冒烟:安装→启动→解析/序列化/指数拒绝/util 加载全通过
- **暴露并修复 bug**:write(Map/List) 值分支缺 BigInteger → 超出 long 的整数序列化抛异常;UTF8/UTF16 各补 2 处 writeBigInt 分支 + 回归测试 serializeBigInteger
- 146 测试全绿;jar 320KB(OSGi manifest 版)
- 经验:OSGi 场景 JDK 内部包(sun.misc)必须 Import-Package 标记 resolution:=optional,否则类加载隔离下找不到;验证 OSGi bundle 用 Felix 框架做冒烟比只看 manifest 可靠

## 根目录文档重写(09:57)

用户要求"简单、基于现有功能",重写 5 个根文档:
- **README.md / README_cn.md**:删除 JavaBean 绑定/注解/过滤器/AutoType 等已删功能的介绍(原文档残留 parseObject(text, Class) 示例、annotations/Filter/autotype/mixin 文档链接);改为纯树模型:支持/已移除清单 + 树模型示例(parseObject/parseArray/parse/toJSONString/toJSONBytes/writeTo/getIntValue/getString/getBooleanValue/getJSONArray/getJSONObject)+ Feature 示例(WriteMapNullValue/PrettyFormat/UseDoubleForDecimals,均已核对真实存在)+ 有效文档索引(index/features/performance/FAQ/ARCHITECTURE/fastjson_1_upgrade)
- **CONTRIBUTING.md**:去除阿里安全应急响应中心 security.alibaba.com 引用与 alibaba 邮箱(用户无找 bug 奖励计划);去掉 kotlin/ASM/checkstyle/多模块等过时内容;改为中文简洁版(构建/测试/风格/PR/Issue/安全私下联系)
- **AGENTS.md**:改为中文、单模块现状:26 源文件结构表(JSON/JSONObject/JSONArray/JSONReader*/JSONWriter*/util 各职责)、行为约定(序列化白名单、1e2 抛异常、BigDecimal 保精度、默认跳过 null、无 Bean API)、构建测试命令、CI 说明
- **CODE_OF_CONDUCT.md**:Contributor Covenant 全文替换为中文极简版(承诺/行为/执行/举报),无邮箱
- 备注:全仓库 grep 无"奖励/bounty"字样,用户所指应即 security.alibaba.com 漏洞奖励平台入口
- **遗留提醒**:ci.yaml 的 test-reflect job 传 -Dfastjson2.creator=reflect 已无作用(反射体系已删),建议删除该 job(已写入 AGENTS.md 提醒,未动 ci.yaml)

## .gitignore 精简(10:01)

用户要求精简 .gitignore:
- 261 行 → 28 行。删除:benchmark/benchmark_21/benchmark_25 相关 6 条、example-graalvm-native 2 条、NetBeans/Vim/Emacs 大段模板、Eclipse CDT/PDT/TeXlipse 等插件特定项、JetBrains 注释块、压缩包(*.zip/*.tar.gz/*.rar/*.nar)、Maven release 产物(pom.xml.tag 等)
- 保留:Maven/Java(target/、*.class、*.jar、hs_err_pid*)、日志临时(*.log/*.tmp/*~)、IDE(.idea/、*.iml、.vscode/、.settings/、.project、.classpath、bin/)、macOS(.DS_Store、._*)、Windows(Thumbs.db、Desktop.ini)
- 删 `!.mvn/wrapper/maven-wrapper.jar`:确认 .mvn/wrapper 只有 properties(新版 only-script wrapper,jar 运行时下载到 ~/.m2/wrapper,项目内无 jar)
- 验证:git check-ignore 关键路径全部命中,无未跟踪文件被误放行
- 附带发现(未动):.omo/run-continuation/*.json(36 个运行数据)与 .workbuddy/memory 已被 git 跟踪;如需停止跟踪 .omo/ 需 git rm -r --cached .omo/ 并加忽略规则
